# ════════════════════════════════════════════════════════════
# SLOPGRAPH Backend — all dependencies (API + ML + Graph)
# Used by Render, Railway, and local dev.
# ════════════════════════════════════════════════════════════

# ── API server ──
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
pydantic-settings>=2.7
python-dotenv>=1.0
requests>=2.32

# ── Graph + ML (Phase 1+) ──
networkx>=3.4
scikit-learn>=1.6
numpy>=2.2
scipy>=1.15

# ── Testing ──
pytest>=8.3
httpx>=0.28
